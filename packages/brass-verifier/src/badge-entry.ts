/**
 * Separate entry point for badge component to avoid forcing React as a runtime dependency
 * 
 * Usage:
 * ```tsx
 * import { BrassBadge } from '@brassproof/verifier/badge'
 * ```
 */

export { BrassBadge, vanillaBadgeScript, type BadgeConfig } from './badge'
